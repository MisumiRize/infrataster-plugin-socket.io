require 'spec_helper'

describe Infrataster::Plugin::Socket.io do
  it 'has a version number' do
    expect(Infrataster::Plugin::Socket.io::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
